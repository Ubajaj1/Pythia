# Kitaru friction log

Every time something breaks, the docs don't answer the question, or a workaround is needed, add an entry. The memo is built from this file.

Context: Pythia is a multi-agent opinion-simulation engine with its own httpx LLM clients (Anthropic, OpenAI, Groq, Ollama), no agent framework, and no tool calls. Kitaru 0.27.1, managed cloud, Python 3.12 virtualenv via uv, macOS.

| When (UTC) | Area | What we tried | What happened | Time lost | Severity | Suggested fix |
|---|---|---|---|---|---|---|
| 2026-09-23 | Install | `uv pip install -e ".[dev,kitaru]"` with `kitaru[cli,worker,mcp]` | Installed in ~1 s (warm uv cache), no conflicts with Pythia's deps (FastAPI, httpx, pydantic). Existing 307 tests still pass. | 0 | positive | None. Worth saying in docs that the extras coexist with FastAPI apps. |
| 2026-09-23 | CLI / doctor | `kitaru doctor` before logging in | Reports `config: pass` and `credentials: pass` with file paths, but neither file exists (`FileNotFoundError` when read). Only `server_resolution` fails. Misleading when debugging setup. | 5 min | minor | Report `missing` (or `skip`) when the file does not exist; only `pass` when it exists and parses. |
| 2026-09-23 | Docs | Looked for a code example of a custom (non-framework) adapter in `docs/…/adapters/custom.md` | Page describes principles ("wrap the public entrypoint") and defers to the `kitaru-adapter-builder` agent skill and the PydanticAI adapter source; no minimal code sample. Had to read installed package source instead. | 20 min | major | Add a 30-line "record a custom agent" example: start session, record one LLM node, finish, read override during replay. |
| 2026-09-23 | Packaging | Looked for user docs inside the installed wheel | The wheel ships contributor-only `AGENTS.md` files (import-linter layering, "new persistent resource checklist") that read like user guidance to a coding agent but aren't. | 5 min | minor | Exclude contributor `AGENTS.md` from the wheel, or replace with a short "using the SDK from an agent" guide. |
| 2026-09-23 | SDK design | Mapped the five capabilities from source | Positive: the SDK is readable and consistent; request/response models are well described. `ReplayOverride.model` accepting a map from old to new model is excellent for multi-model agents: it swaps only Pythia's tick calls. | 0 | positive | Put the model-map override on the replay docs' front page; it's the killer feature for multi-model agents. |
| 2026-09-23 | SDK design | Wanted a synchronous hook for recording LLM calls from inside an existing client wrapper | Recording is async REST calls per batch, so a wrapper must buffer nodes and flush at session end. Fine for us, but every custom adapter will re-implement buffering. | 15 min | minor | Ship a tiny `SessionRecorder` helper (buffer + flush + `model_for(requested)` override lookup) for non-framework agents. |
| 2026-09-24 | Onboarding | `kitaru login` into managed cloud, then `register_agent` and first recorded session | Login and registration were quick (registration ~1 s). First real 50-node session recorded and shown as completed with inputs/outputs and per-node roles. | 0 | positive | None. |
| 2026-09-24 | Data display | Recorded a session without token counts (our wrapper doesn't collect usage yet) | Session shows `cost 0` and all token counts `0` instead of "unknown". A reader would conclude the run was free. | 5 min | minor | Keep `null` as unknown in the UI and rollups; show "not reported" rather than 0. |
| 2026-09-24 | Worker | `kitaru worker start` | Started cleanly and registered with agent, evaluator, importer, and analyzer claims. (Our own `.env` had a stray command line that broke `source .env`; not a Kitaru issue.) | 5 min | positive | None. |
| 2026-09-24 | Replay API | `client.replay(session_id, evaluators=[], ...)` for a plain unchanged replay | Rejected client-side: `evaluators` must have at least one item. Reasonable, but neither the signature nor the replay docs say so; found via pydantic error. The workspace ships 13 built-in evaluators (`kitaru/latency`, `kitaru/cost`, `kitaru/llm-call-signals`, ...) which made the fix easy once found. | 10 min | minor | Default `evaluators` to a sensible built-in set (or document the minimum), and mention the built-ins in the replay guide. |
| 2026-09-24 | Replay API | Replayed a session recorded with `agent_id` but no `agent_version_id` | Server 422: "Session carries no agent version, agent_version_id must be given". Makes sense (the version's run spec is what re-executes), but nothing at record time warns that a session without a version can't be replayed. | 10 min | major | Warn (or default to the latest version) when recording a session without `agent_version_id` for an agent that has a run spec; show "not replayable" in the UI. |
| 2026-09-24 | Replay runtime | Our replay process called `client.get_agent(name)` before creating its session | 403 "Task credentials are not accepted on this route" (agents list is forbidden to task tokens). Correct least-privilege behaviour, and the replay's `error` field helpfully included our process's stderr tail, so it took 2 minutes to find. But the same code path works when recording outside a replay, so custom adapters need two code paths. | 10 min | minor | Document which routes task tokens may call, or let `sessions.create` infer the agent from the token so adapters need one code path. Keep the stderr tail in replay errors; it's excellent. |
| 2026-09-24 | Worker / evaluators | Replay with built-in evaluators `kitaru/latency` and `kitaru/llm-call-signals` | Every evaluator process exited with code 2: the worker runs them via `uv ... --exclude-newer-package kitaru=0 days`, and uv 0.8.8 (Homebrew, Aug 2025) supports the flag but not relative durations. The worker's preflight only checks that the flag exists, so it passes and fails at runtime. The replay then fails before our agent runs. Fixed by putting uv 0.12.18 first on the worker's PATH. | 30 min | major | Check uv's version (or try parsing `0 days`) in the preflight and fail fast with "upgrade uv to ≥ 0.9"; or pass an absolute timestamp instead of `0 days`. |
| 2026-09-24 | Replay (end to end) | Native replay of a recorded 50-call session through our registered agent command | Worked once the four issues above were fixed: the worker ran our script with the baseline inputs, our new session was linked as the replay result automatically, and built-in evaluators ran on both baseline and result. Total 489 s (Groq-bound). Replays over a cohort then ran unattended. | 0 | positive | Lead the docs with this loop for custom agents; it's the product's best moment. |
| 2026-09-24 | Provider limits | Recording 30 sessions on Groq's free tier | Groq allows 8k tokens/min per model; a Pythia session is ~120k tokens, so the experiment would take 7+ hours. Moved the cohort to OpenAI. Not a Kitaru issue, but replay-heavy workflows multiply provider load, so users will hit this. | 30 min | minor | Surface provider rate-limit errors in replay results, and consider a replay concurrency / pacing setting per experiment. |
| 2026-09-24 | Investigations | Created 20 review sessions, then an investigation named `Jev vs LLM: bias (2026-09-24)` | 422: names may contain only letters, digits, `-` and `_`. The rule isn't in the model's field description, and since sessions and the investigation are separate calls, the sessions were already created; a naive retry would duplicate them. We made our script reuse sessions by metadata. | 10 min | minor | State the name rule in `InvestigationCreateRequest.name`'s description (or accept free-text names with a separate slug); consider validating client-side before any call. |
